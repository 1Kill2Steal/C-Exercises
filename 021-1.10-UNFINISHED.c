#include <stdio.h>

// I'm yet to find a solution to this problem (WIP)

int main() {
    /*
    // OLD ATTEMPT (yeah it sucks...):

    int c;

    while ((c = getchar()) != EOF) {

      int placeholder = '\\';
      if (c == '\t')
        putchar(TAB);
      //

      else if (c == '\b')
        putchar('\b');
      //

      else if (c == '\\')
        putchar(back);
      //
      else
        putchar(c);
      //

    }*/
}